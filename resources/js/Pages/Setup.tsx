import { Form } from '@inertiajs/react';
import { PageShell } from '../Components/PageShell';
import { useI18n } from '../i18n';

export default function Setup() {
  const { t } = useI18n();
  return <PageShell><main className="mx-auto max-w-md p-10"><h1 className="text-3xl font-bold">{t('setup.title')}</h1><p className="mt-3 text-sm text-slate-400">{t('setup.help')} <code>docker compose exec app php artisan ipamferry:installation-token</code>.</p><Form action="/setup" method="post" className="mt-6 grid gap-3"><label className="text-sm">{t('setup.token')}<input name="token" type="password" required minLength={32} maxLength={128} pattern="[A-Za-z0-9+/_=-]+" autoComplete="off" spellCheck={false} className="mt-1 w-full rounded bg-slate-800 p-2" /></label><label className="text-sm">{t('setup.name')}<input name="name" type="text" required minLength={2} maxLength={120} autoComplete="name" className="mt-1 w-full rounded bg-slate-800 p-2" /></label><label className="text-sm">{t('setup.email')}<input name="email" type="email" required maxLength={254} pattern="[^\s@]+@[^\s@]+\.[^\s@]+" autoComplete="email" spellCheck={false} onInput={event => { event.currentTarget.value = event.currentTarget.value.replace(/\s/g, ''); }} className="mt-1 w-full rounded bg-slate-800 p-2" /></label><label className="text-sm">{t('setup.password')}<input name="password" type="password" required minLength={14} maxLength={128} autoComplete="new-password" aria-describedby="password-requirements" className="mt-1 w-full rounded bg-slate-800 p-2" /></label><p id="password-requirements" className="text-xs text-slate-400">{t('setup.password_help')}</p><label className="text-sm">{t('setup.confirm')}<input name="password_confirmation" type="password" required minLength={14} maxLength={128} autoComplete="new-password" className="mt-1 w-full rounded bg-slate-800 p-2" /></label><button className="rounded bg-sky-400 px-4 py-2 font-semibold text-slate-950">{t('setup.submit')}</button></Form></main></PageShell>;
}
